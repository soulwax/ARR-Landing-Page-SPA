{
  "name": "arr-landing-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run preview",
    "pm2:dev": "pm2 start ecosystem.config.cjs --only arr-spa-dev --env development",
    "pm2:preview": "pm2 start ecosystem.config.cjs --only arr-spa-preview --env production",
    "pm2:stop": "pm2 delete arr-spa-dev arr-spa-preview || true",
    "pm2:logs": "pm2 logs arr-spa-dev",
    "pm2:logs:preview": "pm2 logs arr-spa-preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "pm2": "^6.0.13",
    "svelte": "^5.43.5",
    "vite": "npm:rolldown-vite@7.2.2"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.2"
  }
}
